[pytest]
pythonpath = ..
filterwarnings =
    ignore::Warning
    default::Warning:seekr_chain
timeout=600
markers =
    interactive: marks tests as interactive (deselect with '-m "not interactive"')
    gpu: marks tests as requiring GPU hardware (run with --gpu)
addopts="--color=yes"
